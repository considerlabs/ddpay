# 인수인계 — ddpay-web (디디페이 APP)

작성일: 2026-08-27

## 1. 프로젝트 개요
- 나름코퍼레이션 "디디페이 APP" 중 **비즈링크(B2B 구매대행)** 화면 프론트엔드 (`plan.pdf` 기획서 참고)
- Next.js 16.2.9 (App Router, Turbopack) + React 19 + Tailwind v4 + shadcn/ui(base-ui) 기반 모바일 웹
- 현재 백엔드 미연동, `lib/mock-data.ts` / `lib/mock-suppliers.ts` 목데이터로 화면 동작

## 2. 실행 방법
```bash
npm install
npm run dev
```
- http://localhost:3000 접속 시 `/login`으로 자동 리다이렉트
- 하단 탭: 홈 / 계약등록(`/contract`) / 비즈링크(`/b2b`) / 카드등록(`/profile/card`) / 내정보(`/profile`)

## 3. 라우트 구조
```
/login
/(main)/home
/(main)/contract               계약등록
/(main)/status                 계약 등록 현황 (오늘 작업 대상)
/(main)/status/[id]            계약 상세
/(main)/payments               결제내역
/(main)/payments/[id]          결제 상세
/(main)/b2b                    비즈링크 홈
/(main)/b2b/supplier-register  공급자 등록
/(main)/b2b/supplier-status    공급자 승인현황 (+ [id] 상세)
/(main)/profile                내정보
/(main)/profile/card(/new)     카드 관리
/(main)/profile/limit          한도 관리
/(main)/profile/password       비밀번호
/(main)/profile/push           알림 설정
/(main)/profile/sender-name    송금자명 등록
/(main)/food                   식자재 새벽배송 안내
```

## 4. 오늘(2026-08-27) 작업 내용 — `app/(main)/status/page.tsx`
계약 등록 현황 화면의 **수동 결제하기** 기능을 신규 구현.

### 4-1. 기본 결제 플로우
- "승인완료" 계약만 결제 버튼 활성화 (승인대기/반려는 비활성 유지)
- 버튼 클릭 → 모달 2단계: **카드선택** → **결제 확인**(받는사람/계좌/송금액/결제금액/결제카드/할부기간 표시) → **결제 완료**
- 결제 완료 시 목록의 `결제완료:N회` 카운트가 로컬 상태로 즉시 반영 (`ManualPaymentSheet` 컴포넌트, 같은 파일 내부)

### 4-2. 일반결제 / 분할결제 (img/1~3.jpeg 참고 + 아래 텍스트 스펙 기반)
> ⚠️ 이번 요청에서 언급된 "첨부 이미지"는 작업 환경에서 신규 파일을 찾지 못해(기존 `img/1~3.jpeg`만 존재) **텍스트 스펙 기준으로 구현**했습니다. 별도 참고 이미지가 있다면 `img/`에 추가 후 스타일 미세조정 요청해 주세요.

- 결제 확인 단계에 **일반결제 / 분할결제 탭** 추가
- **일반결제**: 기존과 동일 (결제금액 직접 입력/수정 후 결제)
- **분할결제**:
  - 결제금액(총액) 입력 시 금액 구간에 따라 입력칸 자동 생성
    | 금액 구간 | 입력칸 수 |
    |---|---|
    | 1천만원 이하 | 3칸 |
    | 3천만원 이하 | 5칸 |
    | 5천만원 이하 | 7칸 |
    | 7천만원 이하 | 9칸 |
    | 9천만원 이하 | 11칸 |
    | **9천만원 초과 (스펙 미정)** | **11칸으로 고정 처리 — 확인 필요** |
  - 마지막 칸 = 총액 − (1~N-1칸 입력 합) 로 자동 계산, 나머지 칸은 직접 입력
  - 전체 칸(마지막 칸 포함)에 금액이 모두 채워지고, 모든 칸의 금액이 서로 달라야만(100원 단위 차이도 인정) "결제하기" 버튼 활성화
  - 동일 금액 입력 시 경고 문구 노출, 합계가 총액을 초과하면 별도 경고 문구 노출

### 4-3. 알려진 제한사항 / TODO
- `img/3.jpeg`의 "자동결제(사용중지)" 표시 케이스는 목데이터에 자동결제 계약이 없어 미구현 (필요 시 `Contract` 타입에 상태 추가 필요)
- 카드 선택 목록은 `mockUser.cards` 1건만 존재 — 실제 다중 카드 연동 시 API 붙이면 자동으로 스와이프 목록 늘어남
- 결제/분할결제 확정 로직은 전부 프론트 mock 처리 (`completedCount` 증가만 수행) — 실제 PG/카드사 연동 API 없음
- 9천만원 초과 구간 입력칸 수는 기획 확정 필요 (현재 11칸 고정 가정)

## 5. 검증
- `npx tsc --noEmit` 통과
- `npx eslint "app/(main)/status/page.tsx"` 통과
- `npm run dev` 로 `/status`에서 승인완료 계약 → 수동 결제하기 → 카드선택 → 결제확인(일반/분할 탭 전환) 수동 확인 완료
